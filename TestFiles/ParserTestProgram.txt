BEGIN MASTER

    Initiate
    {
        // Gøres til globale variable af compileren/preprocessor,
        // så vi ikke har tonsvis af dem.

        pin button1 = createPin(analog, input, A4);
        pin button2 = createPin(digital, input, 5);
        pin button3 = createPin(digital, input, 6);
        pin button4 = createPin(digital, input, 7);
    }

    Listener (button1)
    {
        int filteredInput = 1 / filterNoise(button1, debounce);

        int a = 1 / getValue(button1);

        if (filteredInput == HIGH)
        {
            event button1Pressed = createEvent(filteredInput);
            broadcast(button1Pressed);
        }
        else {
            event button2Pressed = createEvent(filteredInput);
            broadcast(button2Pressed);
        }
    }

    Listener (button2) {
        if (1 > 2) {
            int a = 3;
        }
        else if (2 > 3) {
            int a = 4;
        }
        else {
            int a = 5;
        }

        if (1 > 2) {
            if (5 > 4) {
                int a = 1;
            }
            else {
                if (true) {
                    int a = 3;
                }
                else if (false) {
                    int a = 2;
                }
                else {
                    int a = 1;
                }
            }
        }
        else if (2 > 3) {
            int a = 4;
        }
        else {
            int a = 5;
        }
    }

    Listener (button3) {
        int TEST = -1;
        int a = 1 + (2 + 3);
        int b = (1 + 2) + 3;
        int c = (1 + (2 + (3 + 4)));
        int d = (((1 + 2) + 3) + 4);
        int e = 1 + (2 + 3) + 4;
        int f = (1 + 2) + (3 + 4);
    }

    Listener (button4) {
        int a = 1;
        int b = 2;

        int c = a / b;
        c = a % b;

    }

END MASTER



BEGIN SLAVE : TestSlave1

    Initiate
    {
        pin light1 = createPin(digital, output, 4);
        pin light2 = createPin(digital, output, 5);
        pin light3 = createPin(digital, output, 6);
    }

    EventHandler(button1Pressed)
    {
        int value = getValue(light1);

        if (value == HIGH)
        {
            write(light1, LOW);
        }
        else if (value == LOW)
        {
            write(light1, HIGH);
        }
        else
        {
            int b = 4;
        }
    }

    EventHandler(button2Pressed)
    {
        write(light1, HIGH);
        write(light2, HIGH);
        write(light3, HIGH);
    }

END SLAVE

/* This should
not
be
read */
